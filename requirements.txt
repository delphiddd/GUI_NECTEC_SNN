# Pin ตามเวอร์ชันที่ใช้จริงบนเครื่องพัฒนา (anaconda base, Python 3.11.5)
# Windows: torch จาก PyPI เป็น CPU build ซึ่งพอสำหรับ inference
# ถ้าต้องการ CUDA ให้ลง torch แยกจาก https://pytorch.org/get-started/locally/

torch==2.3.0
torchvision==0.18.0
snntorch==0.9.4

PyQt5==5.15.10

# numpy ต้องเป็น 1.x เท่านั้น — 2.x จะชนกับ torch 2.3.0 และ opencv
numpy<2
opencv-python>=4.6,<5
matplotlib==3.7.5
Pillow==10.4.0
